% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{hmm_inference}
\alias{hmm_inference}
\title{Run HMM inference on diagnostic test data}
\usage{
hmm_inference(
  test_mat,
  method = c("map", "mle", "nuts"),
  nuts_samples = 1000,
  target_acc = 0.65,
  year_process = c("iid", "rw1", "rw2", "ar1", "shrunk", "none"),
  tests = seq_len(6),
  se_fixed = NULL,
  sp_fixed = NULL,
  se_prior_mean = NULL,
  sp_prior_mean = NULL,
  se_prior_ci = NULL,
  sp_prior_ci = NULL,
  hazard_mean = -3,
  hazard_sd = 1.5,
  pi1_a = 1,
  pi1_b = 5,
  penalty = NULL,
  repeat_captures = c("stack", "pool", "last"),
  start_year = NULL,
  seed = 123
)
}
\arguments{
\item{test_mat}{Test matrix (individuals x tests)}

\item{method}{Inference method: "map", "mle", or "nuts"}

\item{nuts_samples}{Number of NUTS samples (for method="nuts")}

\item{target_acc}{Target acceptance rate for NUTS}

\item{year_process}{Year-effect process: \code{"iid"} (default), \code{"rw1"},
\code{"rw2"}, \code{"ar1"}, \code{"shrunk"} or \code{"none"}.}

\item{tests}{Tests to use: a length-6 logical mask or column indices in 1:6.}

\item{se_fixed}{Optional fixed sensitivities (length 6). If supplied, \code{sp_fixed} must also be supplied.}

\item{sp_fixed}{Optional fixed specificities (length 6). If supplied, \code{se_fixed} must also be supplied.}

\item{se_prior_mean}{Optional Se prior means (length 6).}

\item{sp_prior_mean}{Optional Sp prior means (length 6).}

\item{se_prior_ci}{Optional Se prior CIs as 6x2 matrix/list (lo, hi per test).}

\item{sp_prior_ci}{Optional Sp prior CIs as 6x2 matrix/list (lo, hi per test).}

\item{hazard_mean}{Mean for seasonal hazard prior.}

\item{hazard_sd}{SD for seasonal hazard prior.}

\item{pi1_a}{Beta prior alpha for initial prevalence.}

\item{pi1_b}{Beta prior beta for initial prevalence.}

\item{penalty}{Optional logical to enforce Se+Sp>1 soft constraint. If NULL,
defaults to TRUE when Se/Sp are inferred and FALSE when fixed.}

\item{repeat_captures}{Handling of repeat captures within one time-step:
\code{"stack"} (default) keeps each as a separate observation, \code{"pool"} merges
them into one, \code{"last"} keeps only the final capture.}

\item{start_year}{Calendar year of \code{time == 1}. \code{NULL} (default) leaves
\code{year_effect$calendar_year} as \code{NA}.}

\item{seed}{Random seed}
}
\value{
List with individual infection probabilities and prevalence over time
}
\description{
Run HMM inference on diagnostic test data
}
